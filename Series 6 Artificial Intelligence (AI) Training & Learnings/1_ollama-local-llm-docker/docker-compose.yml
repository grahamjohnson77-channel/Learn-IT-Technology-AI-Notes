services:
  ollama:
    image: ollama/ollama:latest
    container_name: 1_ollama_container
    volumes:
      - 1_ollama_volume:/root/.ollama
    ports:
      - "11434:11434"
    restart: unless-stopped

volumes:
  1_ollama_volume:
    name: 1_ollama_models
